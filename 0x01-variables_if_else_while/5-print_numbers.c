#include <stdio.h>

/**
 * main - prints all the single numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%i", i);

		i++;
	}
	printf("\n");

	return (0);
}
