#include "main.h"
#include <string.h>

/**
 * *_strcat - a function that concatenates two strings.
 * @dest: it the first string.
 * @src: it is the second string.
 * Return: return one concatenating string.
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
